{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "deploy:development": "wrangler deploy --minify --env development",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:migrate:local": "wrangler d1 migrations apply portfolio-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply portfolio-db --remote",
    "db:migrate:development": "wrangler d1 migrations apply portfolio-db-development --remote --env development",
    "db:seed:local": "wrangler d1 execute portfolio-db --local --file=seeds/projects.sql",
    "db:seed:remote": "wrangler d1 execute portfolio-db --remote --file=seeds/projects.sql",
    "test": "vitest run --config vitest.config.ts",
    "test:security": "vitest run --config vitest.config.ts test/guestbook-retirement.test.ts test/security-headers.test.ts test/prelaunch-security-review.test.ts"
  },
  "dependencies": {
    "hono": "^4.13.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.110.0"
  }
}
